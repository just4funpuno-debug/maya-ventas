const fs=require('fs');const path='E:/MAYA LIFE AND BEAUTY/INVENTARIO VENTAS/maya-ventas-mvp-vite-no-backticks/src/App.jsx';const lines=fs.readFileSync(path,'utf8').split(/\r?\n/);let depth=0;const out=[];for(let i=0;i<lines.length;i++){const l=lines[i];for(const ch of l){if(ch=='{') depth++; else if(ch=='}') depth--; } if(i%300===0|| i>lines.length-50) out.push({line:i+1,depth,text:l.slice(0,80)});}console.log('Final depth',depth);console.log(out);
