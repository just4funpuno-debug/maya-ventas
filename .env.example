## Variables de entorno requeridas para el build (prefijo VITE_ obligatorio)
# Copia este archivo a `.env` (para desarrollo) o configura las mismas claves en Vercel (Project Settings > Environment Variables)

# URL base de tu proyecto Supabase (Settings > API > Project URL)
VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co

# Public anon key (NO uses la service_role key en el frontend)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.PUBLIC_ANON_KEY_AQUI

# (Opcional) Forzar modo debug sincronización en consola
# VITE_SYNC_DEBUG=true

## Notas
# - Deben existir al momento del build (npm run build) para que Vite las inserte en el bundle.
# - En Vercel crea estas variables para los entornos Production y Preview; luego redeploy.
# - Tras el despliegue abre la consola: debes ver "usingCloud=true".
# - Si ves "usingCloud=false" significa que faltan o no se definieron antes del build.
# Copia este archivo a `.env.local` y rellena con tus credenciales de Supabase
# Nunca publiques tu service_role key en el frontend. Usa solo la anon key aquí.
VITE_SUPABASE_URL=https://TU_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=TU_ANON_PUBLIC_KEY

# Cloudinary (solo para backend/middleware; NO usar prefijo VITE_ para que no se exponga al cliente):
# Estos se usan en los endpoints /api/cloudinary-signature y /api/cloudinary-delete
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret
