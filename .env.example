# Copia este archivo a `.env.local` y rellena con tus credenciales de Supabase
# Nunca publiques tu service_role key en el frontend. Usa solo la anon key aquí.
VITE_SUPABASE_URL=https://TU_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=TU_ANON_PUBLIC_KEY

# Cloudinary (solo para backend/middleware; NO usar prefijo VITE_ para que no se exponga al cliente):
# Estos se usan en los endpoints /api/cloudinary-signature y /api/cloudinary-delete
CLOUDINARY_CLOUD_NAME=tu_cloud_name
CLOUDINARY_API_KEY=tu_api_key
CLOUDINARY_API_SECRET=tu_api_secret
